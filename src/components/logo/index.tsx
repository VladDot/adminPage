import { LogoIcon } from "../../assets";

export const Logo = ({}) => {
    return (
        <div className="content_elements-logo">
            <div className="element_logo-items">
                <LogoIcon />
                <h1>Dashboard</h1>
            </div>

            <h4>v.01</h4>
        </div>
    );
};
